# Phase D 实施方案

**Goal:** 清零 Phase B/C 设计债 — 后端正确性 + UI 金字塔/颜色/静动混合

**Architecture:** pipeline 输出权威 fusion → generator 只渲染；UI 用 DESIGN.md token

## 验收矩阵

| 域 | 标准 | 自动化 |
|----|------|--------|
| functional | 4 Agent 真调用；门控；site 不重算 | pytest |
| ux | price/signal 色；L2 卡片；filter table | ui_acceptance.py |
| ops | 08-STATUS 同步 | check_docs_ssot.py |

### Task D1: 后端正确性

**Files:** `src/orchestrator/pipeline.py`, `src/data/fetcher.py`, `src/site/generator.py`, `tests/`

**验收:** fusion consistency test passes

### Task D2: UI 核心

**Files:** `src/site/generator.py`, `src/site/theme.css`, `src/reports/generator.py`

**验收:** cards show key_basis; momentum uses price colors

### Task D3: 静动 + 交互

**Files:** `src/site/app/*`, generator templates

**验收:** index loads config.js; stock.html static fallback

### Task D4: 数据 + 文档

**Files:** `config/fusion_weights.json`, `docs/design/08-CURRENT-STATUS.md`, `src/api/routes.py`

**验收:** weights sum=1.0; API version 1.5.0
